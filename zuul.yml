configuration:  
  projectName: zuul
server:
  port: 8765
endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false    
cloud:
  config:
    username: user
    password: password
    failFast: true
    discovery:
      enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://eureka-service:8761}/eureka/